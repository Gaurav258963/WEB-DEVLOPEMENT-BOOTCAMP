#include <stdio.h>
int main()
{
    int x;
    int y;
    int z;

    printf("Enter The Positive Number x:\n");
        scanf("%d", &x);
    printf("Enter The Positive Number y:\n");
        scanf("%d", &y);
    printf("Enter The Positive Number z:\n");
        scanf("%d", &z);
    if (x >= y && x >= z)
        
    printf("least no:");
    else printf("Not a least Number");

    return 0;
}